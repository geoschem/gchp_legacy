function formula (args)

'numargs  'args
 numargs = result

       n  = 1
while( n <= numargs )
       qname.n = subwrd(args,n)
       n  = n + 1
endwhile

* Formula:  swgnetcf = swgnetc - swgnet
* -------------------------------------
say 'define qmod = 'qname.1' - 'qname.2
    'define qmod = 'qname.1' - 'qname.2

return
