function formula (args)

'numargs  'args
 numargs = result

       n  = 1
while( n <= numargs )
       qname.n = subwrd(args,n)
       n  = n + 1
endwhile

* Formula:  radnetg = radsrf
* ----------------------------------------------------------------
say 'define qmod = 'qname.1
    'define qmod = 'qname.1

return
