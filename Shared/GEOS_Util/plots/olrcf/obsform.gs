function formula (args)

'numargs  'args
 numargs = result

       n  = 1
while( n <= numargs )
       oname.n = subwrd(args,n)
       n  = n + 1
endwhile

* Formula:  olrcf = olrc - olr
* ----------------------------
say 'define qobs = 'oname.1' - 'oname.2
    'define qobs = 'oname.1' - 'oname.2

return
